services:
  agenda-medica:
    build: .
    container_name: agenda-medica
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./instance:/app/instance
      - ./logs:/app/logs
    restart: unless-stopped
